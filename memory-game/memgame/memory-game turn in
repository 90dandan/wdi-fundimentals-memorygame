console.log("Up and running!");
console.log("user flipped" + cardOne);

const cards = [
{
  rank: 'queen',
  suit: 'hearts',
  cardImage: 'images/queen-of-hearts.png'
},
{
  rank: 'queen',
  suit: 'diamonds',
  cardImage: 'images/queen-of-diamonds.png'
},
{
  rank: 'king',
  suit: 'hearts',
  cardImage: 'images/king-of-hearts.png'
},
{
  rank: 'king',
  suit: 'diamonds',
  cardImage: 'images/king-of-diamonds.png'
},
];
cardsInPlay [];
function checkForMatch(){
  //console.log()
  if (cardsInPlay[0] === cardsInPlay[1]) {
    console.log("You found a match!");
  } else {
    console.log("Sorry, try again.");
  }
}
function flipCard() {
  this.getAttribute('data-id');
  let cardOne = cards[0]
  cardsInPlay.push(cards).rank;
  console.log("User flipped" + cardOne);
  console.log(cardImage);
  console.log(cards).suit;

  let cardTwo = cards[2];
  cardsInPlay.push(cards).rank;
  console.log("User flipped" + cardTwo)

  console.log("User flipped" + cards[cardId].rank);
  cardsInPlay.push("User flipped" + cards["cardId"]);

function createBoard() {
  for (let i = 0; i < cards.length; i++) {
    let newListItem = document.createElement('img');
    cardElement.setAttribute('images/back.png');
    cardElement.setAttribute('data-id',i);
    cardElement.appendChild(game-board);
  }
}
cardElement.addEventListner('click', flipCard());
//is this supposed to go into the function above?
function createBoard()
